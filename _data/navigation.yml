# Sidebar navigation for the design-patterns collection is GENERATED, not listed here.
# See _includes/nav_list — it walks site.design_patterns sorted by `order` and groups by `module`.
# Adding a new pattern post is enough; nothing here needs to change.

main:
  - title: "Learning Map"
    url: /
  - title: "Confusing Pairs"
    url: /design_patterns/confusing-pairs/
